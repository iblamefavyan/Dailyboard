export async function ambilKutipan(){const res=await fetch("https://dummyjson.com/quotes/random");if(!res.ok)throw new Error("Gagal mengambil kutipan");const data=await res.json();return`"${data.quote}" - ${data.author}`}export async function ambilCuaca(kota){const lokasiRes=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(kota)}&count=1&language=id&format=json`);const lokasiData=await lokasiRes.json();if(!lokasiData.results?.length)throw new Error("Kota tidak ditemukan");const lokasi=lokasiData.results[0];const cuacaRes=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${lokasi.latitude}&longitude=${lokasi.longitude}&current=temperature_2m&timezone=auto`);const cuacaData=await cuacaRes.json();return`${lokasi.name}: ${cuacaData.current.temperature_2m}°C`}